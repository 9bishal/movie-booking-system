{
  "build": {
    "builder": "dockerfile",
    "buildCommand": "pip install -r requirements-production.txt && python manage.py collectstatic --noinput --settings=moviebooking.settings_production",
    "startCommand": "gunicorn moviebooking.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120"
  },
  "deploy": {
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyMaxRetries": 10
  }
}
